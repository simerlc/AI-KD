#!/usr/bin/env node

// deploy script is archived: automatic CloudBase deployment removed.
// To restore, extract the original from backups/docs-backup-*.zip

console.log('deploy script is archived and disabled by default.');
console.log('Restore from backups/docs-backup-*.zip to re-enable.');
process.exit(0);
