#!/usr/bin/env node

// setup-tcr script is archived: automatic TCR/CloudBase initialization removed.
// To restore, extract the original from backups/docs-backup-*.zip

console.log('setup-tcr script is archived and disabled by default.');
console.log('Restore from backups/docs-backup-*.zip to re-enable.');
process.exit(0);
